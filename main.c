#include "main.h"
/**
 * main - An entry point
 * @arg: the number of command line arguments
 * @argv: the command line arguments
 * @envp: the environment parameters;
 * Return: 0 on Success
 */
int main(int argc, char **argv, char **enpv)
{
	interactive(enpv);
	return (0);
}
