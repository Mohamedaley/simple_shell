#include "main.h"
#include "main.h"
/**
 * main - prints the name of the program
 * @argc: number of arguments
 * @argv: array of arguments
 * @enpv: environment vars
 * Return: Always 0 (Success)
 */
int main(int argc, char **argv, char **enpv)
{
	interactive(enpv);
	return (0);
}
